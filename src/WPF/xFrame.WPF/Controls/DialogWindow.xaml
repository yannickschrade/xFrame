<Window x:Class="xFrame.WPF.Controls.DialogWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:xFrame.WPF.Controls"
        mc:Ignorable="d"
        x:Name="xDialogWindow"
        Title="{Binding Title}" 
        SizeToContent="WidthAndHeight">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <ContentPresenter x:Name="DialogContentPresenter"/>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Grid.Row="1">
            <Button Content="Cancle"
                    Width="50"
                    Margin="10"
                    Command="{Binding CloseDialogCommand}"
                    CommandParameter="false"
                    Click="OkButton_Click"/>
            
            <Button Content="Ok"
                    Width="50"
                    Margin="10"
                    Command="{Binding CloseDialogCommand}"
                    CommandParameter="true"
                    IsCancel="True"
                    Click="CancleButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
